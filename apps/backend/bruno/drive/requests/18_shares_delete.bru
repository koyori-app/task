meta {
  name: 18 共有取り消し
  type: http
  seq: 18
}

delete {
  url: {{base_url}}/v1/tenants/{{tenant_id}}/drive/folders/{{folder_id}}/shares/{{share_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
