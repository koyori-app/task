meta {
  name: 10 クォータ設定
  type: http
  seq: 10
}

patch {
  url: {{base_url}}/v1/tenants/{{tenant_id}}/drive/quota
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  content-type: application/json
  accept: application/json
}

body:json {
  {
    "quota_bytes": 10737418240
  }
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
