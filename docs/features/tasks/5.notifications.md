---
title: 通知・ウォッチャー
description: in-app/メール通知・ウォッチャー購読・通知設定
icon: lucide:bell
---

# 通知・ウォッチャー 仕様書

> ステータス: **Draft** / 作成日: 2026-05-27
> **PR #5 — 依存: [コア](/features/tasks/core), [コメント・アクティビティ](/features/tasks/collaboration)**

---

## 1. データモデル

### 1.1 `task_watchers`

担当者以外がタスクの変更を購読する。担当者へのアサイン時に自動登録される。

| カラム | 型 | 制約 |
|--------|-----|------|
| `task_id` | UUID | NOT NULL, FK→tasks CASCADE |
| `user_id` | UUID | NOT NULL, FK→users CASCADE |
| `created_at` | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| — | — | PRIMARY KEY(task_id, user_id) |

### 1.2 `notifications`

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| `id` | UUID | PK | |
| `user_id` | UUID | NOT NULL, FK→users CASCADE | 受信者 |
| `task_id` | UUID | NULLABLE, FK→tasks CASCADE | |
| `project_id` | UUID | NULLABLE, FK→projects CASCADE | 可視性の判定に使う。レビュー通知のようにタスクへ紐づかない通知も、プロジェクト単位で絞れる |
| `notification_type` | VARCHAR | NOT NULL | 下表参照 |
| `payload` | JSONB | NOT NULL | 表示に必要なデータ |
| `read_at` | TIMESTAMPTZ | NULLABLE | NULL = 未読 |
| `created_at` | TIMESTAMPTZ | NOT NULL DEFAULT now() | |
| `email_queued_at` | TIMESTAMPTZ | NULLABLE | メール送信待ち。`email_events` に入っている種別だけ立つ。NULL = 送らない |
| `emailed_at` | TIMESTAMPTZ | NULLABLE | 送信済み時刻。NULL = 未送信 |
| `email_attempts` | SMALLINT | NOT NULL DEFAULT 0 | 送信の試行回数。5 回で打ち止め |

通知の行そのものがメールの送信待ち行列（outbox）になっている。通知を作るのは
`service::notifications::create_notification` の 1 箇所で、そこからジョブは積めない
（依存の向きが service → job）ため、行に印を立てて掃き出しループが拾う。

`notification_type` と通知対象:

| type | 通知対象 | トリガー |
|------|---------|--------|
| `assigned` | アサインされたユーザー | 担当者追加 |
| `mentioned` | @メンションされたユーザー | コメント投稿 |
| `deadline_soon` | 担当者 + ウォッチャー | 仮締め / Deadline の 1 日前（バッチ） |
| `status_changed` | ウォッチャー全員 | ステータス変更 |
| `comment_added` | ウォッチャー全員（投稿者除く） | コメント投稿 |
| `pr_merged` | 担当者 + ウォッチャー | GitHub PR マージ |
| `task_blocked` | 担当者 | ブロッキング関係が追加されたとき |
| `review_round_created` | レビューの関係者 + `review_round_any` の購読者 | レビューラウンドの起票 |
| `review_finding_changed` | レビューの関係者 | 指摘の状態遷移 |

`review_round_any` は**通知種別ではなく購読の印**。`in_app_events` に入れた利用者を
`review_round_created` の受信者に加えるだけで、この種別の通知行は作られない。既定は OFF
（既定に入れるとプロジェクト全員が全 PR のラウンドを受け取る）。

### 1.3 `notification_settings`

| カラム | 型 | 制約 |
|--------|-----|------|
| `user_id` | UUID | NOT NULL, FK→users CASCADE |
| `project_id` | UUID | NOT NULL, FK→projects CASCADE |
| `email_events` | VARCHAR[] | NOT NULL DEFAULT '{}' |
| `in_app_events` | VARCHAR[] | NOT NULL DEFAULT '{assigned,mentioned,deadline_soon,comment_added,pr_merged}' |
| — | — | PRIMARY KEY(user_id, project_id) |

---

## 2. マイグレーション

```sql
CREATE TABLE task_watchers (
    task_id UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    PRIMARY KEY (task_id, user_id)
);

CREATE TABLE notifications (
    id UUID PRIMARY KEY,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    task_id UUID REFERENCES tasks(id) ON DELETE CASCADE,
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    notification_type VARCHAR NOT NULL,
    payload JSONB NOT NULL,
    read_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    email_queued_at TIMESTAMPTZ,
    emailed_at TIMESTAMPTZ,
    email_attempts SMALLINT NOT NULL DEFAULT 0
);

CREATE TABLE notification_settings (
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    email_events VARCHAR[] NOT NULL DEFAULT '{}',
    in_app_events VARCHAR[] NOT NULL DEFAULT
        '{assigned,mentioned,deadline_soon,comment_added,pr_merged}',
    PRIMARY KEY (user_id, project_id)
);

CREATE INDEX idx_notifications_user_unread
    ON notifications(user_id, created_at DESC) WHERE read_at IS NULL;

CREATE INDEX idx_notifications_project ON notifications(project_id);

CREATE INDEX idx_notifications_email_pending ON notifications(created_at)
    WHERE email_queued_at IS NOT NULL AND emailed_at IS NULL;
```

---

## 3. 通知生成フロー

```text
タスク更新イベント発生
    │
    ├─ task_watchers から対象ユーザーリストを取得
    ├─ notification_settings で各ユーザーの in_app/email 設定を確認
    ├─ in_app が有効 → notifications テーブルに INSERT
    └─ email が有効 → 同じ行の email_queued_at を立てる
                       （掃き出しループが拾って送る）
```

読み取り（`GET /v1/users/me/notifications`）は `project_id` で視界を絞る。入れない
プロジェクトの通知は一覧にも未読数にも出ない（`project_id IS NULL` の通知は常に見える）。

### 3.1 レビュー通知の受信者

レビューは PR 単位で起きるのでウォッチャーが使えない。宛先は次の 2 つの和から、
**操作した本人**と**そのプロジェクトに入れない利用者**を除いたもの。

1. **関係者** — 同じ PR の全ラウンドから集める: ラウンドの作成者、指摘を修正したと
   宣言した人（`fixed_by`）、解決できた PR の作者。各人の `in_app_events` を見る
2. **購読者**（`review_round_created` のみ） — そのプロジェクトの `in_app_events` に
   `review_round_any` を入れている利用者。設定で明示的に ON にしているので、
   重ねて `in_app_events` は見ない

1 と 2 の両方に当たる利用者でも通知は 1 件。

PR の作者はラウンドの起票時点では分からない（要約ジョブが GitHub から取って
`reviews.pr_author` に控える）。そのため作者への「ラウンドが起票された」通知は
要約ジョブが補う。宛先は `pr_author` が NULL だったラウンドだけで、この列が NULL から
埋まるのはラウンドごとに 1 回きりなので、ジョブを何度走らせても重複しない。
過去ラウンドから作者が判明している場合や購読者の場合は、起票時に作った同じラウンド・
宛先の通知を確認し、要約ジョブから二重に通知しない。

### 3.2 メール送信

`email_events` の `review_round_any` は `review_round_created` の通知もメール対象にする。
全ラウンドを受け取るには `in_app_events` にも `review_round_any` を設定する。
メールは作成済みの in-app 通知から送るため、メール設定だけでは宛先は増えない。

`job::notification_email` の掃き出しループが **30 秒間隔** で、`email_queued_at` が
立っていて `emailed_at` が NULL の行を **最大 50 件** 拾って送る（`FOR UPDATE SKIP LOCKED`
なので複数インスタンスでも二重送信しない）。

- 1 通ごとに確定する（行ごとの短いトランザクションで `FOR UPDATE SKIP LOCKED` を取り、送信と
  その行の更新を commit する）。後の行の失敗で送信済みの印が消えて再送されることはない
- 成功 → `emailed_at` を立てる。1 通の失敗で他の行は止めない
- 失敗 → `email_attempts` を 1 進める。**5 回**で打ち止め（以後は拾わない）
- メール未認証（`users.email_verified = false`）の宛先 / プロジェクトを引けない通知 →
  送らずに `email_queued_at` を NULL に戻して打ち切る

件名・本文は `service::notification_email::render` が種別ごとに日本語で組み立てる。
導線の URL は `email_verification_app_url` を基点に、タスク系は
`/{tenant}/projects/{KEY}/tasks/{KEY}-{seq_id}`、レビュー系は
`/{tenant}/projects/{KEY}/reviews?pr={N}` を出す。

---

## 4. API

### ウォッチャー

| メソッド | パス | 説明 |
|---------|------|------|
| `GET` | `/tasks/{id}/watchers` | 購読者一覧 |
| `POST` | `/tasks/{id}/watchers` | 自分を追加（リクエストボディ不要） |
| `DELETE` | `/tasks/{id}/watchers/{user_id}` | 解除（自分 or テナントオーナー） |

### 通知（ユーザースコープ）

| メソッド | パス | 必要スコープ | 説明 |
|---------|------|-------------|------|
| `GET` | `/v1/users/me/notifications` | 種別に応じて `read:task` / `read:review` | 一覧（未読優先・新着順） |
| `PATCH` | `/v1/users/me/notifications/read-all` | 種別に応じて `write:task` / `write:review` | 権限のある通知を全件既読 |
| `PATCH` | `/v1/users/me/notifications/{id}/read` | 種別に応じて `write:task` / `write:review` | 1 件既読 |

`GET /v1/users/me/notifications` レスポンス:

```json
{
  "unread_count": 3,
  "notifications": [
    {
      "id": "uuid",
      "notification_type": "assigned",
      "project_id": "uuid",
      "task": { "id": "uuid", "seq_id": 42, "title": "OAuth 対応" },
      "payload": { "assigned_by": "田中", "role": "primary" },
      "read_at": null,
      "created_at": "2026-05-27T10:00:00Z"
    }
  ]
}
```

### 通知設定（プロジェクトスコープ）

| メソッド | パス | 必要スコープ | 説明 |
|---------|------|-------------|------|
| `GET` | `/v1/users/me/notification-settings/{project_id}` | `read:task` | 現在の設定取得 |
| `PUT` | `/v1/users/me/notification-settings/{project_id}` | `write:task` | 設定更新 |

`PUT` リクエスト:

```json
{
  "email_events": ["assigned", "deadline_soon"],
  "in_app_events": ["assigned", "mentioned", "deadline_soon", "comment_added", "pr_merged"]
}
```

### PAT からの読み書き

CLI（`task notifications`）は PAT で叩くので、この 5 つの口はセッション専用にしない。
タスク通知には `read:task` / `write:task`、レビュー通知（`review_round_created` /
`review_finding_changed`）には `read:review` / `write:review` が必要。
一覧・未読数は読み取り権限のある種別だけを含み、個別・一括既読は書き込み権限のある
種別だけを対象にする。各操作でどちらのスコープもなければ 403、個別既読で対象の種別に
書き込み権限がなければ 404。既存の上位スコープの包含関係も適用する。
未知の通知種別は PAT に公開しない。通知設定には `read:task` / `write:task` を使う。
CLI の `task notifications settings --project` にキーを渡すと UUID の解決にプロジェクト API を
呼ぶため `read:project` も要る。UUID を渡せば設定 API だけを呼ぶ。

PAT の視界は「PAT のテナントのプロジェクト ∩ `allowed_project_ids`（指定があれば）」を、
セッションと同じ所属判定（入れるプロジェクト）と重ねた集合まで絞る。一覧・未読数・
全件既読の対象・1 件既読の 404 判定は、このプロジェクト範囲と種別ごとの権限を重ねて決める。

`project_id` を持たない通知（この列を足す前に作られた行）は、どのプロジェクトのものか
判別できないため **セッションにだけ** 見せる。PAT からは一覧にも未読数にも出ず、
全件既読の対象にもならない。

---

## 5. フロントエンド（Phase B）

### 通知ベル（ヘッダー）

```text
🔔 3   ← 未読バッジ
```

クリックで通知ドロップダウン:

```text
┌─────────────────────────────────────────┐
│ 通知                         [全て既読] │
├─────────────────────────────────────────┤
│ 🔵 #42 OAuth対応 に担当者追加されました  │
│    田中 → 5分前                          │
├─────────────────────────────────────────┤
│ 🔵 #38 DB設計 にコメントが投稿されました │
│    鈴木 → 1時間前                        │
├─────────────────────────────────────────┤
│ ✓  #30 ログイン の締切が明日です        │
│    → 2時間前                             │
└─────────────────────────────────────────┘
```

### コンポーネント

| コンポーネント | ファイル |
|--------------|---------|
| `NotificationBell` | `components/layout/NotificationBell.vue` |
| `NotificationDropdown` | `components/layout/NotificationDropdown.vue` |
| `NotificationSettingsPage` | `pages/settings/notifications/+Page.vue` |
| `WatcherList` | `components/tasks/WatcherList.vue` |
