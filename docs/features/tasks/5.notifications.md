---
title: 通知・ウォッチャー
description: in-app/メール通知・ウォッチャー購読・通知設定
icon: lucide:bell
---

# 通知・ウォッチャー 仕様書

> ステータス: **Draft** / 作成日: 2026-05-27
> **PR #5 — 依存: [コア](/features/tasks/core), [コメント・アクティビティ](/features/tasks/collaboration)**

---

## 1. データモデル

### 1.1 `task_watchers`

担当者以外がタスクの変更を購読する。担当者へのアサイン時に自動登録される。

| カラム | 型 | 制約 |
|--------|-----|------|
| `task_id` | UUID | NOT NULL, FK→tasks CASCADE |
| `user_id` | UUID | NOT NULL, FK→users CASCADE |
| `created_at` | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| — | — | PRIMARY KEY(task_id, user_id) |

### 1.2 `notifications`

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| `id` | UUID | PK | |
| `user_id` | UUID | NOT NULL, FK→users CASCADE | 受信者 |
| `task_id` | UUID | NULLABLE, FK→tasks CASCADE | task 系の種別だけが持つ |
| `project_id` | UUID | NOT NULL（FK なし） | 通知が属するプロジェクト。一覧の認可の絞り込みに使う |
| `notification_type` | VARCHAR | NOT NULL | 下表参照 |
| `payload` | JSONB | NOT NULL | 表示に必要な非機密データ（actor 名など）。title / body は持たない |
| `target` | JSONB | NOT NULL | 遷移先。`{"type":"task","task_id":…}` |
| `dedupe_key` | VARCHAR | NULLABLE | 冪等化キー。`UNIQUE (user_id, dedupe_key)` |
| `read_at` | TIMESTAMPTZ | NULLABLE | NULL = 未読 |
| `created_at` | TIMESTAMPTZ | NOT NULL DEFAULT now() | |

- 表示文言は保存しない。`notification_type` / `payload` と target の解決結果からクライアントが組み立てる
- `dedupe_key` を持つ通知は `INSERT … ON CONFLICT (user_id, dedupe_key) DO NOTHING` で入れる。
  列の UNIQUE なので NULL 同士は重複扱いされない（部分インデックスは起動時の schema sync と衝突するので使わない）
- 保持期間は `NOTIFICATION_RETENTION_DAYS`（既定 90 日）。日次の cron ジョブ（`job::notification_retention`、
  03:00 UTC）がそれより古い通知を消す。監査ログ・遷移履歴には影響しない

`notification_type` と通知対象:

| type | 通知対象 | トリガー |
|------|---------|--------|
| `assigned` | アサインされたユーザー | 担当者追加 |
| `mentioned` | @メンションされたユーザー | コメント投稿 |
| `deadline_soon` | 担当者 + ウォッチャー | 仮締め / Deadline の 1 日前（バッチ） |
| `status_changed` | ウォッチャー全員 | ステータス変更 |
| `comment_added` | ウォッチャー全員（投稿者除く） | コメント投稿 |
| `pr_merged` | 担当者 + ウォッチャー | GitHub PR マージ |
| `task_blocked` | 担当者 | ブロッキング関係が追加されたとき |

### 1.3 `notification_settings`

| カラム | 型 | 制約 |
|--------|-----|------|
| `user_id` | UUID | NOT NULL, FK→users CASCADE |
| `project_id` | UUID | NOT NULL, FK→projects CASCADE |
| `email_events` | VARCHAR[] | NOT NULL DEFAULT '{}' |
| `in_app_events` | VARCHAR[] | NOT NULL DEFAULT '{assigned,mentioned,deadline_soon,comment_added,pr_merged}' |
| — | — | PRIMARY KEY(user_id, project_id) |

---

## 2. マイグレーション

```sql
CREATE TABLE task_watchers (
    task_id UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    PRIMARY KEY (task_id, user_id)
);

CREATE TABLE notifications (
    id UUID PRIMARY KEY,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    task_id UUID REFERENCES tasks(id) ON DELETE CASCADE,
    notification_type VARCHAR NOT NULL,
    payload JSONB NOT NULL,
    read_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE notification_settings (
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    email_events VARCHAR[] NOT NULL DEFAULT '{}',
    in_app_events VARCHAR[] NOT NULL DEFAULT
        '{assigned,mentioned,deadline_soon,comment_added,pr_merged}',
    PRIMARY KEY (user_id, project_id)
);

CREATE INDEX idx_notifications_user_unread
    ON notifications(user_id, created_at DESC) WHERE read_at IS NULL;
```

増分マイグレーション `m20260924000000_notifications_cursor`:

```sql
ALTER TABLE notifications ADD COLUMN project_id UUID;       -- tasks.project_id から埋めて NOT NULL
ALTER TABLE notifications ADD COLUMN target JSONB;          -- {"type":"task","task_id":…} で埋めて NOT NULL
ALTER TABLE notifications ADD COLUMN dedupe_key VARCHAR;
ALTER TABLE notifications
    ADD CONSTRAINT notifications_user_id_dedupe_key_key UNIQUE (user_id, dedupe_key);
CREATE INDEX idx_notifications_user_created_id
    ON notifications (user_id, created_at DESC, id DESC);
```

`task_id` の無い既存行はプロジェクトを決められないので消す（これまでの生成経路では作られていない）。

---

## 3. 通知生成フロー

```text
タスク更新イベント発生
    │
    ├─ task_watchers から対象ユーザーリストを取得
    ├─ notification_settings で各ユーザーの in_app/email 設定を確認
    ├─ in_app が有効 → notifications テーブルに INSERT
    └─ email が有効 → メール送信ジョブをキューに追加
                       （既存 verification_email ジョブを流用）
```

---

## 4. API

### ウォッチャー

| メソッド | パス | 説明 |
|---------|------|------|
| `GET` | `/tasks/{id}/watchers` | 購読者一覧 |
| `POST` | `/tasks/{id}/watchers` | 自分を追加（リクエストボディ不要） |
| `DELETE` | `/tasks/{id}/watchers/{user_id}` | 解除（自分 or テナントオーナー） |

### 通知（ユーザースコープ）

| メソッド | パス | 説明 |
|---------|------|------|
| `GET` | `/v1/users/me/notifications` | 一覧（新着順・カーソル） |
| `PATCH` | `/v1/users/me/notifications/read-all` | 全件既読 |
| `PATCH` | `/v1/users/me/notifications/{id}/read` | 1 件既読 |

一覧・未読数・既読化は、受信者本人かつ**アクセス可能なプロジェクト**（`project_id`）の通知に
絞る条件を DB クエリに入れる。権限を失ったプロジェクトの通知は行ごと出ず、既読化も 404 になる。

`GET /v1/users/me/notifications` のクエリ:

| クエリ | 意味 |
|---|---|
| `unread` | `true` で未読のみ |
| `kind` | `task` / `review`。`review.` 接頭辞の有無で分ける。それ以外の値は 400 |
| `limit` | 既定 50、上限 100（超える指定は 100 に切る） |
| `cursor` | このカーソルより**古い**行（ページ送り） |
| `after` | このカーソルより**新しい**行（catch-up）。`cursor` と同時指定は 400 |

- 並びは `created_at DESC, id DESC` のみ。未読優先はしない（既読化で順位が変わるとカーソルが成り立たない）。
  未読を上に出したい画面は `unread=true` で別に引く
- **`after` 指定時だけは古い順（`created_at ASC, id ASC`）**で返す。新しい側から `limit` 件を切ると、
  `after` との間の行を引く手段が無くなるため
- `next_cursor` は続きがあるときだけ返る（`null` なら取り切っている）
  - `cursor` 指定時・無指定時: ページ内の最も古い行。次の `cursor` に渡す
  - `after` 指定時: ページ内の最も新しい行。次の `after` に渡す
- 各行の `cursor` はその行自身を指すカーソル。catch-up では受け取った最新行の `cursor` を控え、
  次回 `after` に渡す（`next_cursor` は「取り切った」ときに `null` になるので控えには使えない）
- カーソルは `common::cursor`（`(created_at, id)` を base64 で畳んだもの）。壊れた値は 400
- `unread_count` はクエリに関係なく、見える通知全体の未読数

`GET /v1/users/me/notifications` レスポンス:

```json
{
  "unread_count": 3,
  "next_cursor": "eyJjcmVhdGVkX2F0Ijoi…",
  "notifications": [
    {
      "id": "uuid",
      "notification_type": "assigned",
      "project": { "tenant_id": "uuid", "id": "uuid", "key": "TASK" },
      "task": { "id": "uuid", "seq_id": 42, "title": "OAuth 対応" },
      "payload": { "assigned_by": "田中", "role": "primary" },
      "target": { "type": "task", "task_id": "uuid" },
      "cursor": "eyJjcmVhdGVkX2F0Ijoi…",
      "read_at": null,
      "created_at": "2026-05-27T10:00:00Z"
    }
  ]
}
```

catch-up は取りこぼしを避けるため `unread=true` ではなく `after` を使う（既読だが未受信の行を落とさない）。
`created_at` はトランザクション内の時刻なので、長いトランザクションの通知が後から commit されると
控えたカーソルより古い位置に現れ、`after` では拾えない。現状の生成経路は短いトランザクションなので許容する。

### 通知設定（プロジェクトスコープ）

| メソッド | パス | 説明 |
|---------|------|------|
| `GET` | `/notification-settings` | 現在の設定取得 |
| `PUT` | `/notification-settings` | 設定更新 |

`PUT` リクエスト:

```json
{
  "email_events": ["assigned", "deadline_soon"],
  "in_app_events": ["assigned", "mentioned", "deadline_soon", "comment_added", "pr_merged"]
}
```

---

## 5. フロントエンド（Phase B）

### 通知ベル（ヘッダー）

```text
🔔 3   ← 未読バッジ
```

クリックで通知ドロップダウン:

```text
┌─────────────────────────────────────────┐
│ 通知                         [全て既読] │
├─────────────────────────────────────────┤
│ 🔵 #42 OAuth対応 に担当者追加されました  │
│    田中 → 5分前                          │
├─────────────────────────────────────────┤
│ 🔵 #38 DB設計 にコメントが投稿されました │
│    鈴木 → 1時間前                        │
├─────────────────────────────────────────┤
│ ✓  #30 ログイン の締切が明日です        │
│    → 2時間前                             │
└─────────────────────────────────────────┘
```

### コンポーネント

| コンポーネント | ファイル |
|--------------|---------|
| `NotificationBell` | `components/layout/NotificationBell.vue` |
| `NotificationDropdown` | `components/layout/NotificationDropdown.vue` |
| `NotificationSettingsPage` | `pages/settings/notifications/+Page.vue` |
| `WatcherList` | `components/tasks/WatcherList.vue` |
